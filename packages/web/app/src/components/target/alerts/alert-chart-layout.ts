// Horizontal insets (px) shared by the metric chart's ECharts grid and the
// status-transitions bar so a given time lands at the same x in both.
// LEFT must clear the widest y-axis label so ECharts doesn't clip it.
export const ALERT_CHART_INSET_LEFT = 56;
export const ALERT_CHART_INSET_RIGHT = 12;
