---
'@graphql-hive/render-laboratory': patch
---

Adds a `defaultCollections` option to `renderLaboratory`.

Collections can be pre-populated by passing a list of collections. This is useful when hosted on a gateway.
