---
'hive': patch
---

Fix regressions from moving every button onto the base Button: the "last used" sign-in
indicator no longer hides the button label, the changelog trigger is keyboard reachable as a
single button again, and the laboratory's "New collection" label truncates instead of
overflowing a narrow pane.
