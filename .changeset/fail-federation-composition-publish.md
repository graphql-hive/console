---
'@graphql-hive/cli': minor
'hive': minor

---

Add `schema:publish --fail-on-composition-error` to prevent publishing a federation schema that would cause a composition error.

Closes https://github.com/graphql-hive/console/issues/7588
